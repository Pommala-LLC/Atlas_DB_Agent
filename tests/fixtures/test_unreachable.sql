CREATE PROCEDURE TEST_UNREACHABLE (OUT P_RESULT VARCHAR(20))
LANGUAGE SQL
BEGIN
    IF 1 = 1 THEN
        SET P_RESULT = 'ALWAYS';
        RETURN;
    ELSE
        SET P_RESULT = 'NEVER';
    END IF;
END
